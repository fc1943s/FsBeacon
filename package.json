{
  "scripts": {
    "start": "pwd && ls -al && concurrently \"cd src/FsBeacon.GunPeer && yarn start\" \"cd src/FsBeacon.HubPeer && dotnet run\""
  },
  "devDependencies": {
    "concurrently": "~6.2"
  }
}
