{
  "scripts": {
    "gun":"cd src/FsBeacon.GunPeer && yarn install && yarn start",
    "x": "src/FsBeacon.GunPeer/ssl",
    "start": "yarn gun"
  },
  "devDependencies": {
    "concurrently": "~6.2"
  }
}
